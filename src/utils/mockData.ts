export const mockStats = {
  latPrice: 0.42,
  marketCap: 420000,
  lockedLat: 69000,
  nextMoonMembers: 42,
} as const;

export const mockWalletState = {
  isConnected: false,
  address: "",
  latBalance: 0,
  hasMembership: false,
} as const; 